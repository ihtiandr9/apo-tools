# BKASM TODO

## P0 — Correctness (wrong code = unusable)

- [ ] **Fix ORG** — `size = code_org` должен быть `0`, а не `code_org` (codegen.c:265).  
      При повторном ORG адресация съезжает.
- [ ] **Проверка диапазона immediate** — `MVI A, 200H` должно выдавать ошибку,  
      а не молча усекать до `00H`. RST 8 — то же самое.
- [ ] **Проверка границ буфера `prog[]`** — не писать за пределами `MAX_PROG_SIZE`.
- [ ] **Безопасное копирование в хеш-таблицу** — `strcpy` без проверки длины  
      в `hashtbl.c:22` переполняет буфер при метках > 49 символов.
- [ ] **NULL-безопасность в mathexpr.c** — `lparam`/`rparam` без проверок.

## P1 — Usability (нельзя использовать всерьёз)

- [ ] **Поддержка строчных букв** — `mov a,b` должно работать наравне с `MOV A,B`.  
      Либо `strcasecmp`, либо нормализация в лексере.
- [ ] **`--help` / `-h`** — список опций, краткая справка.
- [ ] **Именованный выходной файл** — `argv[2]` уже открывается как `outfile`,  
      но кодген всё равно пишет в `prog.bin`. Доделать.
- [ ] **Увеличить `MAX_PROG_SIZE`** — 4KB крайне мало. Хотя бы 64KB.
- [ ] **Добавить `.gitignore`** — `Build/*`, `prog.bin`, `test_log.txt`, `*.o`, `*.lst`.

## P2 — Missing features

- [ ] **DS** — резервирование памяти без инициализации (`DS 100`).
- [ ] **INCLUDE** — включение других файлов исходника.
- [ ] **Макросы** — `MACRO`/`ENDM`.
- [ ] **Условная сборка** — `IF`/`ELSE`/`ENDIF`, `IFDEF`.
- [ ] **Скобки в выражениях** — `(1+2)*3`.
- [ ] **SET** — переопределяемые символы (в отличие от EQU).
- [ ] **Listing file** — листинг с адресами и байтами.
- [ ] **Таблица символов** — опция вывода `--sym`.

## P3 — Nice to have

- [ ] **Авто-тесты на ORG, EQU, range checking** — дополнить test_opcodes.
- [ ] **Проверка переопределения меток** — повторное `LABEL:` без ошибки.
- [ ] **Valgrind в CI** — `_build.sh valgrind` уже есть, но автоматизировать.

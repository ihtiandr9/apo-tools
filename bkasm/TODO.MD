## Баги в codegen (codegen.c) — актуально после git pull (92c9dcd)

### Неверный размер инструкций
- [x] **JM** (codegen.c:87) — перенесён в группу 3-байтовых инструкций.
- [x] **RST** (codegen.c:155) — вынесен в отдельный case: `opcode |= (n << 3)`, size=1.

### DW в codegen_generate
- [x] **DW** (codegen.c:27-61) — добавлен case TOK_DW в `codegen_generate`: пишет 2 байта (младший, старший) без opcode.

## Улучшения

- [x] **Комментарии размера** (строки 86, 148, 176) — исправлены; добавлены описания операндов для каждой группы мнемоник.
- [x] **NULL check immediate_param** в DB/DW (строки 27-35) — добавлена проверка с ошибкой.
- [x] **fall through** (строка 154→155) — добавлен комментарий `// fall through`.

## Исправлено в pull (92c9dcd)
- ~~**is_register_pair()**~~ — переписана, использует `opcodes.h` и параметр `arith` для различия SP/PSW
- ~~**Сдвиг регистровых пар**~~ — значения пар в lexer.c изменены (BC=0, DE=2, HL=4, SP=6), теперь `<< 3` даёт правильное кодирование в биты 4-5
- ~~**TOK_PSW**~~ — добавлен как отдельный регистр для PUSH/POP
